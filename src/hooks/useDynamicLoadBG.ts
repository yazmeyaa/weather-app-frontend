//
// import DefaultBackground from 'assets/images/cards/background'
// Get default background if not loaded needed image
//
const useDynamicLoadBG = () =>
    //
    //  First argument is string to search
    //  Second argument is options, that contains onComplete callback or onError callback.
    //
    {
        //
        // Make a state that contains image.
        // Default value for state is DefaultBackground
        //
        // Try to import image like import(...).then(setState(image))
        // If success do onComplete callback
        // If error do onError callback
        //
        // Catch errors if its available
        //
        // Return a state of image and errors.
    }

export { useDynamicLoadBG }
