import { Wrapper, Input, Button } from './searchBar.styles.tsx'

export const SearchBar = () => {
    return (
        <Wrapper>
            <Input></Input>
            <Button>search</Button>
        </Wrapper>
    )
}
