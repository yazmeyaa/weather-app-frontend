import { Wrapper, Input, Button } from './styled'

export const SearchBar = () => {
    return (
        <Wrapper>
            <Input></Input>
            <Button>search</Button>
        </Wrapper>
    )
}
